

**Change**: Reduce the desktop top padding on the hero `<section>` so the text block sits higher, within the white/background area above the artwork.

**File**: `src/components/landing/LandingHero.tsx`

**Edit**: On line 11, change `md:pt-32` to `md:pt-20` (reduce large-screen top padding from 8rem to 5rem). Mobile padding (`pt-10`) stays the same.

