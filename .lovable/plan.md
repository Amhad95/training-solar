

**File:** `src/components/landing/LandingHero.tsx`

Only change: on the section element (line 11), reduce mobile-only top padding from `pt-10` to `pt-4` while keeping `md:pt-32` untouched. No other modifications.

